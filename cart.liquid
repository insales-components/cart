<div class="container">
  {% include "breadcrumb" %}
  <div class="page-title cart-title">
    Корзина
  </div>
  <div class="cart-top" data-cartman-items-text>
  </div>
</div>

<div class="cart-form">
  <div class="container">
    <div class="cart-empty notice notice-info {% unless cart.items.size == 0 %}hide{% endunless %}">
      Корзина пуста
    </div>
  </div>

  {% if cart.items.size > 0 %}
  <div class="container">
    <form action="{{ cart_url }}" method="post" data-cart-form>
      <input type="hidden" name="_method" value="put">
      <input type="hidden" name="make_order" value="">
      <div class="cart-items">
        {% for item in cart.items %}
          {% include "cart_item" %}
        {% endfor %}
      </div>

        <div class="row flex-middle cart-footer">
          <div class="cell-5">
            {% include "discounts" %}
          </div>
          <div class="cell-7">
            <div class="row cart-order flex-middle">
              <div class="cart-total-capture">Итоговый заказ: </div>
              <div class="cart-total" data-cartman-total-price>
                {{ cart.total_price | money }}
              </div>
              <button type="submit"  class="cart-submit btn-secondary in-cart" data-cart-submit>оформить заказ</button>
            </div>
          </div>

        </div>

    </form>
  </div>

  <div class="container">
    {% include "cart_similar" %}
  </div>
  {% endif %}

</div>
